# 实验一收尾与提交清单

> 当前状态：A/B 组 RTL 已落地；本机静态检查、算法边界检查、用户侧 Vivado 行为仿真已通过；Basic Trace 含新版 `cdp-tests` 下 `start` 综合测试已通过。

## 1. 需要保留的验证证据

- Vivado 行为仿真 transcript：包含 `Test Passed!` 和 `$finish called at time : 11480100 ps`。
- Basic Trace 批量测试结果：保留 `run_all_tests.py` 或等价批量运行日志。
- `start` 综合测试记录：旧版 `cdp-tests` 的 Digit 外设地址与 `start.bin` 实际访问地址不一致；更新到最新 `cdp-tests` 后通过，保留新旧框架对比和通过日志即可。
- 静态检查结果：`python tools/check_minirv_static.py` 输出 `STATIC CHECK PASSED`。
- 算法边界检查结果：`python tools/verify_minirv_algorithms.py` 输出 `ALGORITHM CHECK PASSED (16 cases)`。
- Synthesis 日志：综合成功，0 errors、0 critical warnings。
- Implementation 日志：实现/布线成功，0 errors、0 critical warnings。
- Timing Summary：`All user specified timing constraints are met`。

## 2. Vivado 报告截图

按指导书最终提交要求，继续在 Vivado 中补齐：

1. Run Synthesis。
2. 截图或导出 Utilization Report。
3. Run Implementation。
4. 截图或导出 Timing Summary，重点保留 WNS/TNS 等时序结论。
5. 截图或导出 Power Report。
6. 若要求下板或 bitstream，则 Generate Bitstream，并保留完成截图。
7. 若有 Critical Warning，报告中说明是否与本实验 CPU RTL 相关；时钟/IP 相关 warning 需单独留意。

当前保留的模板 warning 说明：

- `VRFC 10-3091 actual bit length ... addra` 来自老师模板 `Inst_ROM/Data_RAM` 中 `addr[31:2]` 连接到 BRAM IP 较窄地址端口。Vivado 自动截断未使用高位；该 warning 未影响行为仿真、Basic Trace、综合或实现结果。为保持老师模板结构，不为清理该 warning 修改模板存储器封装。

## 3. 实验报告建议结构

1. 实验目标：A/B 组合计 36 条指令，保留模板 8 条指令回归。
2. 数据通路与控制：引用 A/B 数据通路表、控制信号表。
3. 模块化实现：
   - `Controller/defines`
   - `SEXT/NPC`
   - `ALU`
   - `MREQ/MEXT`
   - `multiplier/divider`
   - `cpu_core` 多周期提交与 Trace 信号
4. 乘除法说明：
   - 乘法器使用 Booth 补码迭代。
   - 除法器使用恢复除法迭代。
   - 未使用 `*`、`/` 或 IP 实现乘除运算。
5. 验证结果：
   - 静态检查通过。
   - 算法边界检查通过。
   - Vivado 行为仿真通过。
   - Basic Trace 含新版 `start` 综合测试通过。
6. 已知问题：
   - 旧版 `cdp-tests` 中 Digit 外设地址与 `start.bin` 实际访问地址不一致；已通过更新测试框架解决。
7. 综合/实现结果：
   - Utilization
   - Timing
   - Power

## 4. 提交包建议

优先提交源码与报告，不提交完整 Vivado 生成目录。

当前已准备：

- 最新源码 zip 重新生成后以 `dist/single_cycle_*.zip` 中时间最新者为准。
- `lab1/A_B组模块化Verilog实现落地方案.md`
- `lab1/指导书通读要点记录.md`
- A/B 数据通路表与控制信号表

最终提交前确认：

- zip 小于 100 MB。
- 不含 `.runs/.cache/.sim/.hw/ip_user_files` 等 Vivado 生成目录。
- 报告导出为 PDF。
- 若报告提到 `start` 早期失败，说明其为旧版 `cdp-tests` 框架地址配置问题，并附新版通过结果。
