# miniRV 数据通路图文档

依据代码整理：`lab1/miniRV_basic_ego1/miniRV_basic/src/rtl/*.v`。本文只描述组件、端口和信号连接关系，不绘制图形。

## 总体组件和端口列表

### *miniRV_SoC* 端口

| 方向 | 端口 | 位宽 | 说明 |
|---|---|---:|---|
| input | `fpga_clk` | 1 | FPGA 输入时钟 |
| input | `fpga_rst` | 1 | FPGA 复位，低有效 |
| input | `sw` | 16 | 开关输入，当前数据通路未使用 |
| output | `led` | 16 | LED 输出，当前数据通路未使用 |
| output | `dig_en` | 8 | 数码管位选，当前数据通路未使用 |
| output | `dig_seg` | 8 | 数码管段选，当前数据通路未使用 |
| output | `dig_seg1` | 8 | 数码管段选，当前数据通路未使用 |
| input | `rx` | 1 | UART 接收，当前数据通路未使用 |
| output | `tx` | 1 | UART 发送，当前数据通路未使用 |

内部组件：`clk_wiz_0 U_clkgen`、`cpu_top U_cpu`。

### *cpu_top* 端口

| 方向 | 端口 | 位宽 | 说明 |
|---|---|---:|---|
| input | `cpu_clk` | 1 | CPU 时钟 |
| input | `cpu_rst` | 1 | CPU 复位，高有效 |

内部组件：`cpu_core U_core`、`Inst_ROM U_irom`、`Data_RAM U_dram`。

### *cpu_core* 端口

| 方向 | 端口 | 位宽 | 说明 |
|---|---|---:|---|
| input | `cpu_rst` | 1 | CPU 核复位 |
| input | `cpu_clk` | 1 | CPU 核时钟 |
| output | `ifetch_req` | 1 | 取指请求 |
| output | `ifetch_addr` | 32 | 取指地址 |
| input | `ifetch_valid` | 1 | 指令有效 |
| input | `ifetch_inst` | 32 | 指令数据 |
| output | `daccess_ren` | 4 | 数据读使能 |
| output | `daccess_addr` | 32 | 数据访存地址 |
| input | `daccess_rvalid` | 1 | 数据读有效 |
| input | `daccess_rdata` | 32 | 数据读出值 |
| output | `daccess_wen` | 4 | 数据写使能 |
| output | `daccess_wdata` | 32 | 数据写入值 |
| input | `daccess_wresp` | 1 | 数据写响应 |

内部组件：`NPC U_NPC`、`PC U_PC`、`Controller U_CU`、`RF U_RF`、`SEXT U_SEXT`、`ALU U_ALU`、`MREQ U_MEM_REQ`、`MEXT U_MEM_EXT`。

### *PC* 端口

| 方向 | 端口 | 位宽 | 说明 |
|---|---|---:|---|
| input | `clk` | 1 | 时钟 |
| input | `rst` | 1 | 复位 |
| input | `npc` | 32 | 下一条 PC |
| input | `fetch` | 1 | PC 更新使能 |
| output | `pc` | 32 | 当前 PC |

### *NPC* 端口

| 方向 | 端口 | 位宽 | 说明 |
|---|---|---:|---|
| input | `op` | 2 | 下一 PC 选择 |
| input | `pc` | 32 | 当前 PC |
| input | `base` | 32 | JALR 基址 |
| input | `offset` | 32 | 跳转或分支偏移 |
| input | `br` | 1 | 分支条件结果 |
| output | `npc` | 32 | 下一条 PC |
| output | `pc4` | 32 | `pc + 4` |

### *Controller* 端口

| 方向 | 端口 | 位宽 | 说明 |
|---|---|---:|---|
| input | `opcode` | 7 | 指令 opcode |
| input | `funct3` | 3 | 指令 funct3 |
| input | `funct7` | 7 | 指令 funct7 |
| output | `npc_op` | 2 | NPC 控制 |
| output | `sext_op` | 3 | 立即数扩展控制 |
| output | `alua_sel` | 1 | ALU A 输入选择 |
| output | `alub_sel` | 1 | ALU B 输入选择 |
| output | `alu_op` | 5 | ALU 运算控制 |
| output | `is_mul` | 1 | 乘法类指令标志 |
| output | `is_div` | 1 | 除法类指令标志 |
| output | `ram_r_op` | 3 | 访存读扩展控制 |
| output | `ram_w_op` | 4 | 访存写使能控制 |
| output | `rf_we` | 1 | 寄存器堆写使能控制 |
| output | `rf_wsel` | 2 | 写回数据来源选择 |

### *RF* 端口

| 方向 | 端口 | 位宽 | 说明 |
|---|---|---:|---|
| input | `clk` | 1 | 时钟 |
| input | `rR1` | 5 | 读寄存器 1 地址 |
| input | `rR2` | 5 | 读寄存器 2 地址 |
| input | `we` | 1 | 写使能 |
| input | `wR` | 5 | 写寄存器地址 |
| input | `wD` | 32 | 写寄存器数据 |
| output | `rD1` | 32 | 读寄存器 1 数据 |
| output | `rD2` | 32 | 读寄存器 2 数据 |

### *SEXT* 端口

| 方向 | 端口 | 位宽 | 说明 |
|---|---|---:|---|
| input | `op` | 3 | 立即数格式选择 |
| input | `imm` | 25 | 指令立即数字段 `inst[31:7]` |
| output | `ext` | 32 | 扩展后的立即数 |

### *ALU* 端口

| 方向 | 端口 | 位宽 | 说明 |
|---|---|---:|---|
| input | `rst` | 1 | 复位 |
| input | `clk` | 1 | 时钟 |
| input | `op` | 5 | 运算控制 |
| input | `a` | 32 | 运算数 A |
| input | `b` | 32 | 运算数 B |
| output | `c` | 32 | 运算结果 |
| output | `br` | 1 | 分支判断结果 |
| output | `busy` | 1 | 乘除法忙信号 |

### *MREQ* 端口

| 方向 | 端口 | 位宽 | 说明 |
|---|---|---:|---|
| input | `ram_addr` | 32 | 原始访存地址 |
| input | `ram_rop` | 3 | 读访存类型 |
| output | `da_ren` | 4 | 数据 RAM 读使能 |
| output | `da_addr` | 32 | 数据 RAM 地址 |
| input | `ram_wop` | 4 | 写访存类型 |
| input | `ram_wdata` | 32 | 原始写数据 |
| output | `da_wen` | 4 | 数据 RAM 写使能 |
| output | `da_wdata` | 32 | 对齐后的写数据 |

### *MEXT* 端口

| 方向 | 端口 | 位宽 | 说明 |
|---|---|---:|---|
| input | `op` | 3 | 读数据扩展类型 |
| input | `din` | 32 | 数据 RAM 读出值 |
| input | `byte_offs` | 2 | 字节偏移 |
| output | `ext` | 32 | 扩展后的读数据 |

### *Inst_ROM* 端口

| 方向 | 端口 | 位宽 | 说明 |
|---|---|---:|---|
| input | `cpu_clk` | 1 | 时钟 |
| input | `cpu_rst` | 1 | 复位 |
| input | `inst_rreq` | 1 | 取指读请求 |
| input | `inst_addr` | 32 | 取指地址 |
| output | `inst_valid` | 1 | 指令有效 |
| output | `inst_out` | 32 | 指令输出 |

### *Data_RAM* 端口

| 方向 | 端口 | 位宽 | 说明 |
|---|---|---:|---|
| input | `cpu_clk` | 1 | 时钟 |
| input | `cpu_rst` | 1 | 复位 |
| input | `data_ren` | 4 | 读使能 |
| input | `data_addr` | 32 | 访存地址 |
| output | `data_valid` | 1 | 读数据有效 |
| output | `data_rdata` | 32 | 读数据 |
| input | `data_wen` | 4 | 写使能 |
| input | `data_wdata` | 32 | 写数据 |
| output | `data_wresp` | 1 | 写响应 |

### *multiplier* 端口

| 方向 | 端口 | 位宽 | 说明 |
|---|---|---:|---|
| input | `clk` | 1 | 时钟 |
| input | `rst` | 1 | 复位 |
| input | `x` | `WIDTH` | 被乘数 |
| input | `y` | `WIDTH` | 乘数 |
| input | `start` | 1 | 开始 |
| output | `z` | `2*WIDTH` | 乘法结果 |
| output | `busy` | 1 | 忙信号 |

### *divider* 端口

| 方向 | 端口 | 位宽 | 说明 |
|---|---|---:|---|
| input | `clk` | 1 | 时钟 |
| input | `rst` | 1 | 复位 |
| input | `x` | `WIDTH` | 被除数 |
| input | `y` | `WIDTH` | 除数 |
| input | `start` | 1 | 开始 |
| output | `z` | `WIDTH` | 商 |
| output | `r` | `WIDTH` | 余数 |
| output | `busy` | 1 | 忙信号 |

## 数据通路连接

### *miniRV_SoC* 端口连接

`miniRV_SoC.fpga_clk ---- fpga_clk ---- clk_wiz_0.clk_in1`

`clk_wiz_0.clk_out1 ---- pll_clk1 ---- sys_clk`

`clk_wiz_0.locked ---- pll_lock ---- sys_clk/sys_rst生成逻辑`

`sys_clk ---- sys_clk ---- cpu_top.cpu_clk`

`sys_rst ---- sys_rst ---- cpu_top.cpu_rst`

### *cpu_top* 端口连接

`cpu_top.cpu_clk ---- cpu_clk ---- cpu_core.cpu_clk`

`cpu_top.cpu_rst ---- cpu_rst ---- cpu_core.cpu_rst`

`cpu_top.cpu_clk ---- cpu_clk ---- Inst_ROM.cpu_clk`

`cpu_top.cpu_rst ---- cpu_rst ---- Inst_ROM.cpu_rst`

`cpu_top.cpu_clk ---- cpu_clk ---- Data_RAM.cpu_clk`

`cpu_top.cpu_rst ---- cpu_rst ---- Data_RAM.cpu_rst`

`cpu_core.ifetch_req ---- cpu2ic_rreq ---- Inst_ROM.inst_rreq`

`cpu_core.ifetch_addr ---- cpu2ic_addr ---- Inst_ROM.inst_addr`

`Inst_ROM.inst_valid ---- ic2cpu_valid ---- cpu_core.ifetch_valid`

`Inst_ROM.inst_out ---- ic2cpu_inst ---- cpu_core.ifetch_inst`

`cpu_core.daccess_ren ---- cpu2dc_ren ---- Data_RAM.data_ren`

`cpu_core.daccess_addr ---- cpu2dc_addr ---- Data_RAM.data_addr`

`Data_RAM.data_valid ---- dc2cpu_valid ---- cpu_core.daccess_rvalid`

`Data_RAM.data_rdata ---- dc2cpu_rdata ---- cpu_core.daccess_rdata`

`cpu_core.daccess_wen ---- cpu2dc_wen ---- Data_RAM.data_wen`

`cpu_core.daccess_wdata ---- cpu2dc_wdata ---- Data_RAM.data_wdata`

`Data_RAM.data_wresp ---- dc2cpu_wresp ---- cpu_core.daccess_wresp`

### *cpu_core.IF* 端口连接

`PC.pc ---- pc ---- cpu_core.ifetch_addr`

`first_req/inst_finished_r ---- ifetch_req生成逻辑 ---- cpu_core.ifetch_req`

`PC.pc ---- pc ---- NPC.pc`

`NPC.npc ---- npc ---- PC.npc`

`inst_finished ---- inst_finished ---- PC.fetch`

`NPC.pc4 ---- pc4 ---- 写回选择rf_wD`

### *cpu_core.ID* 端口连接

`cpu_core.ifetch_inst ---- ifetch_inst ---- inst`

`inst[6:0] ---- opcode ---- Controller.opcode`

`inst[14:12] ---- funct3 ---- Controller.funct3`

`inst[31:25] ---- funct7 ---- Controller.funct7`

`Controller.npc_op ---- npc_op ---- NPC.op`

`Controller.sext_op ---- sext_op ---- SEXT.op`

`Controller.alu_op ---- alu_op ---- ALU.op`

`Controller.alua_sel ---- alua_sel ---- ALU输入A选择`

`Controller.alub_sel ---- alub_sel ---- ALU输入B选择`

`Controller.ram_r_op ---- ram_rop ---- MREQ.ram_rop`

`Controller.ram_w_op ---- ram_wop ---- MREQ.ram_wop`

`Controller.rf_we ---- rf_we ---- rf_we1生成逻辑`

`Controller.rf_wsel ---- rf_wsel ---- rf_wD选择逻辑`

`inst[19:15] ---- rs1 ---- RF.rR1`

`inst[24:20] ---- rs2 ---- RF.rR2`

`inst[31:7] ---- imm ---- SEXT.imm`

`SEXT.ext ---- ext ---- NPC.offset`

`SEXT.ext ---- ext ---- ALU输入B选择`

`SEXT.ext ---- ext ---- RF.wD选择逻辑`

`RF.rD1 ---- rf_rd1 ---- NPC.base`

`RF.rD1 ---- rf_rd1 ---- ALU输入A选择`

`RF.rD2 ---- rf_rd2 ---- ALU输入B选择`

`RF.rD2 ---- rf_rd2 ---- MREQ.ram_wdata`

### *cpu_core.EX* 端口连接

`PC.pc ---- pc ---- ALU输入A选择`

`RF.rD1 ---- rf_rd1 ---- ALU输入A选择`

`ALU输入A选择 ---- alu_a ---- ALU.a`

`SEXT.ext ---- ext ---- ALU输入B选择`

`RF.rD2 ---- rf_rd2 ---- ALU输入B选择`

`ALU输入B选择 ---- alu_b ---- ALU.b`

`Controller.alu_op ---- alu_op ---- ALU.op`

`ALU.br ---- br ---- NPC.br`

`ALU.c ---- alu_c ---- MREQ.ram_addr`

`ALU.c ---- alu_c ---- RF.wD选择逻辑`

`ALU.busy ---- mul_div_busy ---- 乘除法完成控制`

### *cpu_core.MEM* 端口连接

`ALU.c ---- alu_c ---- MREQ.ram_addr`

`Controller.ram_r_op ---- ram_rop ---- MREQ.ram_rop`

`Controller.ram_w_op ---- ram_wop ---- MREQ.ram_wop`

`RF.rD2 ---- rf_rd2 ---- MREQ.ram_wdata`

`MREQ.da_ren ---- da_ren ---- cpu_core.daccess_ren`

`MREQ.da_addr ---- da_addr ---- cpu_core.daccess_addr`

`MREQ.da_wen ---- da_wen ---- cpu_core.daccess_wen`

`MREQ.da_wdata ---- da_wdata ---- cpu_core.daccess_wdata`

`cpu_core.daccess_rdata ---- daccess_rdata ---- MEXT.din`

`ram_rop_r ---- ram_rop_r ---- MEXT.op`

`alu_c_r[1:0] ---- byte_offs ---- MEXT.byte_offs`

`MEXT.ext ---- ram_ext ---- RF.wD选择逻辑`

`cpu_core.daccess_rvalid ---- daccess_rvalid ---- ld_st_done生成逻辑`

`cpu_core.daccess_wresp ---- daccess_wresp ---- ld_st_done生成逻辑`

### *cpu_core.WB* 端口连接

`rf_we1生成逻辑 ---- rf_we1 ---- RF.we`

`inst[11:7]/rf_wR_r ---- rf_wR ---- RF.wR`

`ALU.c ---- alu_c ---- RF.wD`

`NPC.pc4 ---- pc4 ---- RF.wD`

`SEXT.ext ---- ext ---- RF.wD`

`MEXT.ext ---- ram_ext ---- RF.wD`

`rf_wD选择逻辑 ---- rf_wD ---- RF.wD`

### *Inst_ROM* 内部端口连接

`Inst_ROM.cpu_clk ---- cpu_clk ---- IROM.clka`

`Inst_ROM.inst_addr[31:2] ---- inst_addr[31:2] ---- IROM.addra`

`IROM.douta ---- inst_out ---- Inst_ROM.inst_out`

`Inst_ROM.inst_rreq ---- inst_rreq ---- inst_valid生成逻辑`

`inst_valid生成逻辑 ---- inst_valid ---- Inst_ROM.inst_valid`

### *Data_RAM* 内部端口连接

`Data_RAM.cpu_clk ---- cpu_clk ---- DRAM.clka`

`Data_RAM.data_addr[31:2] ---- data_addr[31:2] ---- DRAM.addra`

`DRAM.douta ---- data_rdata ---- Data_RAM.data_rdata`

`Data_RAM.data_wen ---- data_wen ---- DRAM.wea`

`Data_RAM.data_wdata ---- data_wdata ---- DRAM.dina`

`Data_RAM.data_ren ---- data_ren ---- data_valid生成逻辑`

`Data_RAM.data_wen ---- data_wen ---- data_wresp生成逻辑`

`data_valid生成逻辑 ---- data_valid ---- Data_RAM.data_valid`

`data_wresp生成逻辑 ---- data_wresp ---- Data_RAM.data_wresp`

### *ALU* 内部乘除法端口连接

`ALU.clk ---- clk ---- multiplier U_mul.clk`

`ALU.rst ---- rst ---- multiplier U_mul.rst`

`ALU.a ---- a ---- multiplier U_mul.x`

`ALU.b ---- b ---- multiplier U_mul.y`

`mul_flag ---- mul_flag ---- multiplier U_mul.start`

`multiplier U_mul.z ---- mul_res ---- ALU.c选择逻辑`

`multiplier U_mul.busy ---- mul_busy ---- ALU.busy生成逻辑`

`ALU.clk ---- clk ---- multiplier U_mulu.clk`

`ALU.rst ---- rst ---- multiplier U_mulu.rst`

`{1'b0, ALU.a} ---- {1'b0,a} ---- multiplier U_mulu.x`

`{1'b0, ALU.b} ---- {1'b0,b} ---- multiplier U_mulu.y`

`mulu_flag ---- mulu_flag ---- multiplier U_mulu.start`

`multiplier U_mulu.z ---- mulu_res ---- ALU.c选择逻辑`

`multiplier U_mulu.busy ---- mulu_busy ---- ALU.busy生成逻辑`

`ALU.clk ---- clk ---- divider U_div.clk`

`ALU.rst ---- rst ---- divider U_div.rst`

`a_abs ---- a_abs ---- divider U_div.x`

`b_abs ---- b_abs ---- divider U_div.y`

`div_flag ---- div_flag ---- divider U_div.start`

`divider U_div.z ---- div_quo ---- ALU.c选择逻辑`

`divider U_div.r ---- div_rem ---- ALU.c选择逻辑`

`divider U_div.busy ---- div_busy ---- ALU.busy生成逻辑`

`ALU.clk ---- clk ---- divider U_divu.clk`

`ALU.rst ---- rst ---- divider U_divu.rst`

`ALU.a ---- a ---- divider U_divu.x`

`ALU.b ---- b ---- divider U_divu.y`

`divu_flag ---- divu_flag ---- divider U_divu.start`

`divider U_divu.z ---- divu_quo ---- ALU.c选择逻辑`

`divider U_divu.r ---- divu_rem ---- ALU.c选择逻辑`

`divider U_divu.busy ---- divu_busy ---- ALU.busy生成逻辑`

